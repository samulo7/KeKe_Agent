# 实施计划：公司 AI Agent 系统一期 MVP

## Context

当前仓库只有需求与技术方案文档，尚无实现代码。本次实施计划的目标，是把 `Company_Agent_PRD_v1.5.md` 与 `tech-stack.md` 落成一个可执行的一期 MVP，并且优先完成“基础功能”，再进入完整业务功能。

本计划面向 AI 开发者执行，要求：
- 只按 PRD 一期范围实施：请假审批、报销审批、知识库问答。
- 先打牢基础：文档基线、目录结构、认证、权限、数据模型、集成骨架、日志与队列。
- 每一步都必须可验证；未通过验证不得进入下一步。
- 严格模块化，多文件拆分，禁止把路由、业务规则、集成、状态处理、UI 全部塞进单体巨文件。
- 若 PRD 与技术栈文档冲突，以 PRD 为准。

## 已确认且不得擅自改变的约束

- 权威需求来源：`E:\git\KeKe_Agent\Company_Agent_PRD_v1.5.md`
- 技术选型参考：`E:\git\KeKe_Agent\tech-stack.md`
- 一期范围仅含：请假审批、报销审批、知识库问答。
- 审批链由钉钉模板/流程定义，系统只负责发起、同步、展示，不自定义审批流。
- 登录方式固定为钉钉 SSO + 服务端 Session + HttpOnly Cookie。
- 系统角色只表达后台权限与数据范围，不表达审批链含义。
- 请假和报销都是“对话式承接钉钉现有表单”，不是重新发明流程。
- 一期不做：企业微信接入、可配置审批流引擎、ERP 对接、移动 App、复杂假种规则、外出审批接入。
- 知识库回答必须附来源：文档名 + 业务分类 + 上传时间 + 引用定位。
- 若知识库没有相关文档，必须明确返回“未找到相关文档”，不能用通用 LLM 知识补答案。

## 当前可复用资产

当前仓库没有现成应用代码、函数或工具实现；只有以下文档可复用为实施依据：
- `E:\git\KeKe_Agent\Company_Agent_PRD_v1.5.md`
- `E:\git\KeKe_Agent\tech-stack.md`
- `E:\git\KeKe_Agent\CLAUDE.md`

因此本次实施按“绿地项目”方式推进，但所有模块边界、数据结构、流程规则必须从上述文档抽取，不可凭空发明。

## 推荐模块边界（执行时保持多文件拆分）

建议建立以下模块层次，并始终按职责拆分：
- `memory-bank/`：架构文档、业务设计文档、决策记录
- `apps/web/`：React 前端（对话入口 + 管理后台）
- `apps/api/`：Express API 服务
- `apps/worker/`：BullMQ Worker
- `packages/shared/`：共享枚举、Schema、类型、常量、错误码
- `packages/integrations/`：钉钉、DashScope、OCR、OSS、SMTP、Redis 客户端封装
- `packages/domain/`：请假、报销、知识库、用户权限等领域服务
- `packages/data/`：数据库访问、迁移、仓储
- `packages/test/`：测试夹具、模拟数据、端到端测试辅助

执行要求：
- 不允许把多个领域流程写进一个总控大文件。
- 不允许把 SDK 直接散落在 UI、路由、业务函数里。
- 不允许把数据库结构、状态机、权限规则只藏在代码里而不更新到 `memory-bank/@architecture.md`。

## 关键实施路径

先完成“基础功能”，再进入“完整功能”。

---

# 第一阶段：基础功能（必须先完成）

## 步骤 1：建立 memory-bank 基线文档
**目标**：满足项目规则要求，在写业务代码前先固化架构基线和业务基线。

**指令**：
1. 创建 `memory-bank/@architecture.md`，整理系统边界、模块划分、核心表、状态机、集成关系、队列职责、部署形态。
2. 创建 `memory-bank/@game-design-document.md`，这里虽然不是游戏项目，但沿用该文件名作为“业务交互设计文档”，整理请假、报销、知识库三大流程的对话交互与状态流。
3. 明确标记“一期范围”“一期不做”“二期预留”。
4. 在文档中记录“审批链由钉钉负责”的不可变规则。

**关键路径**：
- `memory-bank/@architecture.md`
- `memory-bank/@game-design-document.md`

**验证**：
- 逐项对照 PRD 第 2、3、6、10、12 章，确认无遗漏、无新增范围。
- 人工检查两份 memory-bank 文档是否能独立解释系统架构与业务流程。
- 未通过前，不进入任何代码脚手架工作。

## 步骤 2：固定目录结构与模块边界
**目标**：在项目开始时就避免单体巨文件和混乱目录。

**指令**：
1. 按推荐模块边界创建前端、后端、Worker、shared、integrations、domain、data、test 的顶层结构。
2. 为每个目录写一句职责说明，记录进 `memory-bank/@architecture.md`。
3. 明确每类改动应落在哪一层，不允许跨层堆叠。
4. 为以后新增模块定义命名约定和责任边界。

**关键路径**：
- `memory-bank/@architecture.md`
- `apps/web/`
- `apps/api/`
- `apps/worker/`
- `packages/shared/`
- `packages/integrations/`
- `packages/domain/`
- `packages/data/`
- `packages/test/`

**验证**：
- 检查目录是否能容纳前端、API、异步任务而不相互耦合。
- 抽查任意一个未来功能点，确认能明确归属到一个模块层，而不是必须塞进单文件。

## 步骤 3：固定环境变量与部署前提
**目标**：先明确所有外部依赖，避免实现中途返工。

**指令**：
1. 根据技术栈文档列出所有环境变量：DashScope、OCR、OSS、数据库、Redis、钉钉、SMTP、Session。
2. 区分“本地开发必填”“联调必填”“上线必填”。
3. 标记哪些变量仅服务端可见，绝不出现在前端构建产物里。
4. 将变量用途与来源记录进 `memory-bank/@architecture.md`。

**关键路径**：
- `memory-bank/@architecture.md`
- 环境配置说明文件（执行时自行落盘）

**验证**：
- 对照 `tech-stack.md` 第七部分环境变量清单，确认无缺失。
- 检查每个外部系统至少有一组对应凭证或占位项。
- 检查敏感变量没有被规划到前端可访问位置。

## 步骤 4：确定前后端启动骨架
**目标**：先打通最小可运行骨架，而不是一开始就写业务细节。

**指令**：
1. 搭建前端应用壳，仅保留路由、基础布局、登录占位页、对话页占位、后台页占位。
2. 搭建 API 服务壳，仅保留健康检查、配置加载、错误边界、日志入口。
3. 搭建 Worker 服务壳，仅保留队列连接、健康检查、空任务处理框架。
4. 不要在这一步写任何请假/报销/知识库具体规则。

**关键路径**：
- `apps/web/`
- `apps/api/`
- `apps/worker/`

**验证**：
- 前端可启动并显示基础页面骨架。
- API 服务可启动并返回健康检查结果。
- Worker 可启动并连接 Redis。
- 三个进程独立运行，不依赖业务功能才能启动。

## 步骤 5：定义共享枚举与领域常量
**目标**：把最容易漂移的业务常量提前收敛成共享定义。

**指令**：
1. 提炼角色枚举、业务状态枚举、报销分类枚举、请假类型枚举、通知类型枚举、错误码分类。
2. 这些共享定义放在 `packages/shared/`，禁止复制到多个模块。
3. 在 `memory-bank/@architecture.md` 写明共享常量的唯一来源。
4. 明确哪些值未来可扩展，哪些值在一期必须固定。

**关键路径**：
- `packages/shared/`
- `memory-bank/@architecture.md`

**验证**：
- 对照 PRD 第 3、4、6、8 章，确认每个固定枚举都能找到来源。
- 检查请假/报销/权限/状态没有出现重复定义计划。

## 步骤 6：设计数据库初版结构
**目标**：先落实主数据结构与状态机承载字段。

**指令**：
1. 以 PRD 第 6、10 章和 tech-stack 第五部分为依据，设计 `users`、`leave_requests`、`expense_reports`、`invoices`、`documents`、`document_chunks`、`approval_logs`、`email_logs`、`audit_logs`。
2. 对 `leave_requests` 和 `expense_reports` 固定共享状态字段：`status`、`approval_instance_id`、`current_node_name`、`current_approver`、`last_synced_at`、`rejection_reason`。
3. 为报销重提设计 revision 机制。
4. 为知识库版本管理设计“文件名 + 业务分类”的版本识别逻辑。
5. 为回调幂等设计唯一约束。

**关键路径**：
- `packages/data/`
- `memory-bank/@architecture.md`

**验证**：
- 做一轮表结构审查，确认所有一期主流程都能落库。
- 逐条映射 PRD 字段，确认没有缺少余额告警、OCR 失败、拒绝原因、版本状态等关键字段。
- 确认设计中没有审批链节点定义表，因为审批链不归系统维护。

## 步骤 7：设计数据库迁移与初始化流程
**目标**：保证数据库可重复初始化，便于 AI 开发与测试。

**指令**：
1. 定义数据库迁移顺序与回滚策略。
2. 定义开发环境种子数据：角色、示例员工、示例审批状态、示例知识库分类。
3. 为 pgvector 初始化单独留出扩展与索引步骤。
4. 把数据库初始化步骤写入实施文档。

**关键路径**：
- `packages/data/`
- `packages/test/`
- `memory-bank/@architecture.md`

**验证**：
- 在空数据库上重复执行初始化，确认结构一致。
- 检查 pgvector 扩展、索引、种子数据均能建立。
- 检查种子数据不会违反权限和状态约束。

## 步骤 8：实现钉钉 SSO 登录骨架
**目标**：优先落实固定认证方式，作为所有功能前提。

**指令**：
1. 设计钉钉登录入口、回调处理、失败处理、会话建立、退出登录流程。
2. 登录成功后仅建立服务端 Session，不建立本地密码体系。
3. 规划 Session 与 HttpOnly Cookie 的生命周期和刷新策略。
4. 为未登录访问预留统一拦截与跳转逻辑。

**关键路径**：
- `apps/api/` 认证模块
- `apps/web/` 登录入口与会话恢复模块
- `packages/integrations/` 钉钉认证客户端
- `packages/domain/` 会话领域服务

**验证**：
- 未登录访问受保护页面时被正确拦截。
- 登录回调成功后服务端存在 Session，前端能识别登录态。
- Session 失效后页面表现符合预期，不出现脏权限数据。

## 步骤 9：实现用户同步与角色落位
**目标**：把钉钉用户信息映射为系统用户与默认角色。

**指令**：
1. 设计首次登录自动建档或更新流程。
2. 按 PRD 规则，新员工首次登录默认角色为员工。
3. 用户主数据来源固定为钉钉，系统不维护独立主数据。
4. 规划管理员后台修改角色的入口，但先只做最小可用版本。

**关键路径**：
- `packages/domain/` 用户领域模块
- `packages/integrations/` 钉钉组织接口模块
- `packages/data/` 用户仓储

**验证**：
- 首次登录用户能够自动落库。
- 重复登录不会创建重复用户。
- 角色默认值符合 PRD。
- 当钉钉组织信息缺失时，系统给出明确阻断提示，而不是自行兜底审批人。

## 步骤 10：实现权限与数据范围骨架
**目标**：先落实“谁能看什么、谁能操作什么”。

**指令**：
1. 按 PRD 第 4 章建立角色到权限矩阵。
2. 区分“操作权限”和“数据可见范围”。
3. 把全员视图、HR 余额维护、知识库文档管理、手动补偿、审计查看拆开。
4. 权限判断放入独立模块，不要散落在页面和路由里。

**关键路径**：
- `packages/domain/` 权限模块
- `packages/shared/` 权限枚举
- `apps/api/` 权限中间件
- `apps/web/` 页面级权限守卫

**验证**：
- 员工只能看到本人单据。
- HR 能访问余额维护但不能访问全员报销汇总。
- 管理员能访问文档管理与手动补偿。
- 任一越权访问都被后端阻止，而不是只靠前端隐藏按钮。

## 步骤 11：实现钉钉客户端与 Token 缓存层
**目标**：把所有钉钉调用封装到独立集成层。

**指令**：
1. 单独建立钉钉 Access Token 获取与缓存策略。
2. 封装员工信息读取、审批实例创建、审批状态查询、工作通知发送四类调用。
3. 不允许业务模块直接拼接钉钉 API 请求。
4. 为后续回调验签与状态查询预留独立能力。

**关键路径**：
- `packages/integrations/` 钉钉客户端
- `packages/domain/` 调用适配层

**验证**：
- 模拟过期 Token 场景，确认可自动恢复。
- 任意业务流程只通过统一客户端访问钉钉。
- 日志中能区分调用成功、失败、重试来源。

## 步骤 12：实现统一错误、审计、结构化日志框架
**目标**：先有可追踪性，再做复杂业务。

**指令**：
1. 统一定义业务错误、集成错误、权限错误、校验错误、系统错误分类。
2. 为关键动作落审计：登录、角色变更、手动补偿、知识库文档管理。
3. 为集成链路落结构化日志：钉钉、SMTP、OCR、OSS、LLM、数据库。
4. 明确哪些日志不得包含敏感信息。

**关键路径**：
- `packages/shared/` 错误与日志常量
- `packages/domain/` 审计模块
- `packages/data/` 审计与日志落库模块
- `memory-bank/@architecture.md`

**验证**：
- 每类错误都能落到统一响应格式。
- 审计事件能按操作人、时间、对象检索。
- 敏感字段未出现在日志中。

## 步骤 13：实现邮件与队列基础设施
**目标**：先把异步能力打通，避免后面业务功能返工。

**指令**：
1. 搭建 Redis 连接与 BullMQ 基础能力。
2. 创建 `email-queue`、`sync-queue`、`ocr-queue`，`reminder-queue` 先保留可选。
3. 实现 SMTP 客户端封装与邮件发送日志落库。
4. 固定邮件失败 3 次指数退避重试策略。

**关键路径**：
- `apps/worker/`
- `packages/integrations/` Redis 与 SMTP 模块
- `packages/domain/` 队列调度模块
- `packages/data/` `email_logs`

**验证**：
- 队列能够接收并消费测试任务。
- 邮件失败时会进入重试，而不是直接丢失。
- 邮件日志能正确记录成功、失败、重试次数。

## 步骤 14：实现钉钉回调安全骨架
**目标**：先把最关键的外部入口保护好。

**指令**：
1. 单独挂载审批回调入口。
2. 实现签名校验、时间戳窗口校验、`nonce` 防重放。
3. 以 `approval_instance_id + event_id` 作为幂等键写入 `approval_logs`。
4. 明确乱序回调处理规则：只接受最新 `event_time`。

**关键路径**：
- `apps/api/` 回调模块
- `packages/domain/` 回调幂等与安全模块
- `packages/data/` `approval_logs`

**验证**：
- 重放同一事件不会二次更新业务状态。
- 错误签名、超时戳、重复 `nonce` 都被拒绝。
- 合法回调能落日志并通过幂等表追踪。

---

# 第二阶段：基础业务功能（优先完成完整闭环）

## 步骤 15：落地请假领域模型
**目标**：优先完成一期最基础、规则相对清晰的业务闭环。

**指令**：
1. 定义请假单实体、状态字段、余额告警字段、时间粒度字段。
2. 区分会话态与正式单据态；未确认提交前不得生成员工可见 DRAFT。
3. 为“查看本月记录”“查看余额”预留查询接口。
4. 不实现工龄计算与复杂假种引擎。

**关键路径**：
- `packages/domain/leave/`
- `packages/data/` 请假仓储
- `packages/shared/` 请假枚举与状态

**验证**：
- 能创建和读取请假业务对象。
- 未确认提交前不会落正式单据。
- 余额告警字段能被正确记录。

## 步骤 16：实现请假对话字段采集规则
**目标**：先把对话输入稳稳映射到钉钉表单字段。

**指令**：
1. 实现假种识别或追问。
2. 按假种限制日期/时间字段输入方式。
3. 实现冲突检测：日期、时间段、时长不一致时必须重问。
4. 汇总确认前不得调用钉钉创建审批。

**关键路径**：
- `packages/domain/leave/` 对话解析模块
- `packages/shared/` 请假字段规则
- `apps/web/` 对话页状态模块

**验证**：
- “请年假”“请明天上午年假”“请调休 4 小时”等典型输入能进入正确分支。
- “明天下午请半天假 4 小时”会被识别为冲突并阻断提交。
- 假种不同，表单必填字段不同。

## 步骤 17：实现请假审批创建链路
**目标**：完成从对话确认到钉钉审批实例创建。

**指令**：
1. 在用户确认后生成业务单号并创建请假单。
2. 调用钉钉创建审批实例，记录 `approval_instance_id`。
3. 建立创建幂等，避免重复提交生成多个审批实例。
4. 创建成功后推送钉钉工作通知给当前审批人。

**关键路径**：
- `packages/domain/leave/` 提交模块
- `packages/integrations/` 钉钉审批客户端
- `packages/data/` 请假单仓储

**验证**：
- 同一确认动作重复触发，不会创建重复审批实例。
- 创建成功后单据状态进入 `APPROVING`。
- 当前审批节点信息能回填到业务单据。

## 步骤 18：实现请假状态同步与通知闭环
**目标**：完成请假主链路闭环。

**指令**：
1. 在回调中更新请假单状态、当前节点、审批人、同步时间。
2. 支持通过、拒绝、撤回三类状态变化。
3. 回调触发审批结果邮件通知。
4. 实现“查看我本月请假记录”和详情展示。

**关键路径**：
- `packages/domain/leave/` 状态同步模块
- `packages/domain/notification/`
- `apps/web/` 请假列表与详情页面

**验证**：
- APPROVING、APPROVED、REJECTED、CANCELLED 四类状态都能显示正确。
- 撤回仅允许在 APPROVING 状态执行。
- 审批通过/拒绝邮件能在规定时间内被排队发送。

## 步骤 19：实现 HR 请假余额维护最小功能
**目标**：补上请假流程依赖的后台基础数据。

**指令**：
1. 实现 HR 专员查看员工余额与编辑余额的最小后台界面/接口。
2. 管理员不可修改余额。
3. 每次修改写审计日志。
4. 对话查询余额时明确标注数据来源为 HR 后台维护值。

**关键路径**：
- `packages/domain/leave/` 余额模块
- `apps/web/` HR 后台页面
- `packages/data/` 余额仓储/审计

**验证**：
- HR 可修改，管理员和普通员工不可修改。
- 修改后请假对话能读取到最新余额。
- 审计日志记录修改人、时间、前后值。

---

# 第三阶段：完整业务功能（基础闭环稳定后继续）

## 步骤 20：落地报销领域模型与双表单骨架
**目标**：建立报销基础对象，先分离差旅与日常两条表单。

**指令**：
1. 设计 `expense_reports` 主体、分类字段、表单类型字段、revision 结构、OCR 标记、丢失原票标记。
2. 把差旅报销与日常报销拆成独立表单配置，不要混成一个大表单。
3. 为“查看我本月报销状态”“查看全员报销汇总”分别设计不同查询口径。
4. 固定报销分类只含“差旅报销”“日常报销”。

**关键路径**：
- `packages/domain/expense/`
- `packages/shared/` 报销枚举
- `apps/web/` 报销页面骨架

**验证**：
- 进入差旅报销与日常报销时显示不同字段集合。
- 财务视图与员工个人视图的查询范围不同。
- revision 结构可支撑拒绝后重提。

## 步骤 21：实现发票上传与 OSS 存储链路
**目标**：让报销单能够承接真实凭证文件。

**指令**：
1. 设计发票/凭证上传入口，仅允许图片与 PDF。
2. 文件先落 OSS，再记录文件元数据到数据库。
3. 规划私有 Bucket 与临时访问签名策略。
4. 不要把文件直接存数据库。

**关键路径**：
- `packages/integrations/` OSS 客户端
- `packages/domain/expense/` 附件模块
- `packages/data/` `invoices`

**验证**：
- 上传后文件可在 OSS 找到，数据库有对应元数据记录。
- 非法文件类型被拒绝。
- 已上传文件可通过后端受控方式读取。

## 步骤 22：实现 OCR 异步处理与人工回补
**目标**：让发票录入既能自动提取，也能在失败时继续业务。

**指令**：
1. 上传完成后把 OCR 任务投递到 `ocr-queue`。
2. 提取发票号码、日期、金额、税额、买卖方信息。
3. OCR 失败时设置 `ocr_failed = true`，允许人工录入全部字段继续提交。
4. OCR 结果与人工修正都要可追踪。

**关键路径**：
- `apps/worker/` OCR 任务处理
- `packages/integrations/` OCR 客户端
- `packages/domain/expense/` OCR 回写模块

**验证**：
- 正常发票上传后能回填识别结果。
- OCR 故障时单据不会卡死，仍可人工补录。
- 财务端可筛选 `ocr_failed` 单据。

## 步骤 23：实现报销字段规则与凭证校验
**目标**：把系统可判断的规则放在提交前完成。

**指令**：
1. 实现差旅和日常报销各自的必填字段检查。
2. 实现模糊事由识别与重填提示。
3. 实现合计金额不得超过票据金额的校验。
4. 实现餐补 >160 需替票、电子发票去重、丢失原票上限 50% 等规则。
5. 将“可自动校验”和“财务人工核查”分开实现，不要混淆。

**关键路径**：
- `packages/domain/expense/` 规则引擎
- `packages/data/` 发票去重查询
- `packages/shared/` 报销规则常量

**验证**：
- 模糊事由会被拦截。
- 重复电子发票会被拒绝。
- 丢失原票场景会自动限制金额上限。
- 超标住宿/餐补会提示但允许带原因继续提交。

## 步骤 24：实现报销审批创建与重提机制
**目标**：完成报销主链路最核心的创建和拒绝后重提。

**指令**：
1. 提交报销后创建业务单与钉钉审批实例。
2. 状态进入 `APPROVING`，记录 `approval_instance_id`。
3. 拒绝后允许在原业务单上修改并重提，同时保存 revision 历史。
4. 重提时废弃旧审批实例关联，创建新实例。

**关键路径**：
- `packages/domain/expense/` 提交与重提模块
- `packages/integrations/` 钉钉审批客户端
- `packages/data/` 报销仓储

**验证**：
- 首次提交成功后能追踪当前审批节点。
- REJECTED 单据可回到 DRAFT 并修改。
- 重提后 revision 历史完整保留，默认展示最新版本。

## 步骤 25：实现报销状态同步、列表、详情、汇总
**目标**：完成报销业务闭环与财务视图。

**指令**：
1. 通过回调/轮询更新报销状态与节点展示字段。
2. 员工端实现个人列表与详情。
3. 财务端实现全员报销汇总、按分类统计、按节点统计。
4. 拒绝原因、当前节点、当前审批人都要可见。

**关键路径**：
- `packages/domain/expense/` 状态同步模块
- `apps/web/` 员工报销页面
- `apps/web/` 财务汇总页面

**验证**：
- 员工只能看自己的单据，财务/管理员可看汇总。
- 列表状态与详情节点信息一致。
- 拒绝原因可展示且与回调内容一致。

## 步骤 26：落地知识库文档管理与版本机制
**目标**：先把文档生命周期跑通。

**指令**：
1. 实现管理员上传 PDF / Word 文档。
2. 上传时要求选择业务分类。
3. 以“文件名 + 业务分类”识别版本；同键新文档上传后旧版标为历史版本，不物理删除。
4. 普通员工只有查询权限，无上传权限。

**关键路径**：
- `packages/domain/knowledge/` 文档管理模块
- `packages/data/` `documents`
- `apps/web/` 管理员文档后台

**验证**：
- 管理员可上传，员工不可上传。
- 同键新文档上传后旧文档状态正确变为历史版本。
- 文档元信息包含文件名、业务分类、版本、上传时间。

## 步骤 27：实现文档解析、切片、向量化入库
**目标**：完成知识库检索前的数据准备链路。

**指令**：
1. 分别处理 PDF 与 Word 的解析。
2. 将文档切片后写入 `document_chunks`。
3. 使用 `text-embedding-v3` 生成向量并写入 pgvector。
4. 记录每个切片的来源定位信息，以便回答时引用。

**关键路径**：
- `packages/domain/knowledge/` 解析与切片模块
- `packages/integrations/` Embedding 客户端
- `packages/data/` `document_chunks`

**验证**：
- 上传文档后能够生成切片与向量记录。
- PDF 切片可对应页码；Word 切片可对应段落或标题。
- 历史版本文档不参与默认最新检索。

## 步骤 28：实现知识库问答与引用格式
**目标**：完成知识库员工查询主链路。

**指令**：
1. 实现提问向量化、相似度检索、上下文组装、回答生成。
2. 回答必须附：文档名、业务分类、上传时间、引用定位。
3. 若低于置信度阈值，必须提示查看原文，不给确定性结论。
4. 若完全无文档命中，必须返回“未找到相关文档”。

**关键路径**：
- `packages/domain/knowledge/` 检索与回答模块
- `packages/integrations/` LLM 客户端
- `apps/web/` 知识库问答页面

**验证**：
- 普通提问可返回答案与引用。
- 低置信度场景不会输出武断答案。
- 空结果场景返回明确未找到，不使用通用知识兜底。

---

# 第四阶段：可靠性、后台能力与上线前完善

## 步骤 29：实现轮询兜底与重试链路
**目标**：补全回调丢失、外部依赖失败场景。

**指令**：
1. 用 `sync-queue` 每 10 分钟扫描 `APPROVING` 且 `last_synced_at` 超时的单据。
2. 主动拉取钉钉最新状态并回写。
3. 为审批创建失败、邮件失败、OCR 失败分别落实重试与人工处理路径。
4. 不要用进程内 `setTimeout` 代替队列。

**关键路径**：
- `apps/worker/`
- `packages/domain/` 同步与重试模块
- `packages/data/` 相关日志/状态仓储

**验证**：
- 模拟回调丢失后，10 分钟内能恢复状态一致性。
- 邮件失败能自动重试并留下日志。
- 审批创建失败不会产生重复实例污染。

## 步骤 30：实现管理员手动补偿与审计查询
**目标**：为极端异常留最后兜底，但严格受控。

**指令**：
1. 后台实现手动补偿入口，只允许管理员使用。
2. 仅允许将 `APPROVING` 修正到 `APPROVED` / `REJECTED` / `CANCELLED`。
3. 操作前强制填写单据 ID、变更前状态、变更后状态、原因。
4. 全量写入 `audit_logs`，并提供审计查询。

**关键路径**：
- `packages/domain/` 补偿与审计模块
- `apps/web/` 管理后台
- `packages/data/` `audit_logs`

**验证**：
- 非管理员无法访问补偿入口。
- 已终态单据不能被任意回退。
- 每次补偿都能在审计页检索到完整记录。

## 步骤 31：补齐通知、后台可观测性和运维页面
**目标**：让系统进入可运营状态。

**指令**：
1. 实现邮件日志查询与重放入口。
2. 实现 OCR 失败筛选、回调日志查看、队列状态查看。
3. 视需要接入 BullMQ Dashboard，仅作为内部运维使用。
4. 所有后台页面按角色权限隔离。

**关键路径**：
- `apps/web/` 管理后台
- `apps/api/` 运维查询接口
- `packages/data/` 运维查询仓储

**验证**：
- 管理员可重放失败邮件。
- 可检索 OCR 失败和回调失败记录。
- 运维页面不向普通员工暴露。

## 步骤 32：建立测试体系与 UAT 对照矩阵
**目标**：把 PRD 验收门槛直接转成可执行验证清单。

**指令**：
1. 按 PRD 第 12 章拆出 P0 / P1 / P2 测试用例矩阵。
2. 覆盖请假、报销、知识库主链路及异常链路。
3. 为每条用例标注前置条件、输入、期望结果、是否自动化。
4. 目标不少于 120 条核心链路测试用例。

**关键路径**：
- `packages/test/`
- `memory-bank/@architecture.md`
- 单独的 UAT 追踪文档（执行时落盘）

**验证**：
- P0 用例 100% 通过。
- 整体通过率达到 PRD 目标。
- 每一条 PRD 验收门槛都能映射到至少一条测试用例。

## 步骤 33：性能、安全、并发与数据保留验证
**目标**：在上线前验证非功能要求。

**指令**：
1. 验证普通对话、知识库问答、审批创建、邮件发送的性能指标。
2. 验证 20 人并发在线的关键链路表现。
3. 验证最小权限原则、日志脱敏、敏感信息不入 LLM。
4. 验证数据保留策略：审计日志、对话日志、请假报销记录、发票图片。

**关键路径**：
- `apps/api/`
- `apps/worker/`
- `packages/domain/`
- `packages/data/`
- `memory-bank/@architecture.md`

**验证**：
- p95 指标符合 PRD 第 9 章。
- 并发压测达到 20 人目标。
- 抽查日志与请求载荷，确认无身份证、工资等敏感信息传给 LLM。
- 数据保留策略在文档和实现中一致。

## 步骤 34：完成部署编排、回滚预案与上线检查单
**目标**：让系统具备可上线、可回滚、可排障的最小运维能力。

**指令**：
1. 固化 ECS + PM2 + Nginx + Redis 的部署顺序。
2. 明确 API、Worker、Redis、数据库、OSS、SMTP、钉钉依赖检查顺序。
3. 编写回滚预案：数据库迁移失败、回调异常、队列积压、外部服务不可用时的处理步骤。
4. 上线前最后更新 `memory-bank/@architecture.md`，确保与实际实现一致。

**关键路径**：
- 部署说明文档
- `memory-bank/@architecture.md`

**验证**：
- 在预发环境完整跑一遍部署与回滚演练。
- 断开任一外部依赖时，系统能给出可观测的失败记录。
- 架构文档与实际模块、表结构、队列、集成点一致。

---

# 执行顺序建议

按以下顺序推进，不要跳步：
1. 步骤 1–14：基础功能
2. 步骤 15–19：请假闭环
3. 步骤 20–25：报销闭环
4. 步骤 26–28：知识库闭环
5. 步骤 29–34：可靠性、UAT、部署

原则：
- 前一阶段验证未通过，不进入后一阶段。
- 每完成一个重大里程碑，必须更新 `memory-bank/@architecture.md`。
- 若实现过程中发现 PRD 缺口，先记录问题并确认，不要自行扩展范围。

## 关键修改路径总览

由于当前仓库尚无代码，预计会新增或持续修改的关键路径包括：
- `memory-bank/@architecture.md`
- `memory-bank/@game-design-document.md`
- `apps/web/`
- `apps/api/`
- `apps/worker/`
- `packages/shared/`
- `packages/integrations/`
- `packages/domain/leave/`
- `packages/domain/expense/`
- `packages/domain/knowledge/`
- `packages/domain/auth/`
- `packages/domain/permission/`
- `packages/data/`
- `packages/test/`

## 总体验证方式（端到端）

在实施阶段，每个里程碑至少执行以下验证：
- 启动验证：前端、API、Worker 都能独立启动。
- 数据验证：数据库迁移、向量扩展、种子数据、关键表字段完整。
- 集成验证：钉钉登录、审批创建、回调同步、工作通知、SMTP、OCR、OSS、LLM/RAG。
- 权限验证：员工、HR、财务、管理员各自只见应见数据。
- 业务验证：请假、报销、知识库三条主链路全通。
- 异常验证：回调重放、回调乱序、邮件失败、OCR 失败、审批创建失败、轮询兜底。
- UAT 验证：严格对照 PRD 第 12 章逐项勾验。
- 文档验证：`memory-bank/@architecture.md` 与实际实现保持同步。

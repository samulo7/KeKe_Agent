# 公司 AI Agent 系统

产品需求文档（PRD）

MVP 基线 · v1.5 · 唯一执行口径

| 字段 | 内容 |
| :---- | :---- |
| **文档版本** | v1.5 |
| **创建日期** | 2026-03 |
| **文档状态** | 可评审 / 可执行 |
| **替代文档** | v1.0.docx · v1.1 修订稿 · v1.2 · v1.3 Final · v1.4 |
| **一期范围** | 请假审批 · 报销审批 · 知识库问答 |
| **技术栈** | React + Node.js + 通义千问 Qwen + 阿里云 RDS PostgreSQL/pgvector |
| **核心集成** | 钉钉（SSO · 审批 · 组织）· 网易企业邮箱 SMTP |

---

# 1. 背景与目标

## 1.1 项目背景

公司当前在人事与费用流程上存在重复操作、审批不透明、通知易遗漏、知识分散等问题。本项目建设统一 AI Agent 平台，用对话式入口承接高频内部流程，优先完成可落地且可稳定交付的 MVP。

## 1.2 一期目标

- 员工可通过统一对话入口发起请假、报销、知识查询
- 审批在钉钉内闭环，系统内可追踪状态与审计轨迹
- 通知自动化（审批结果）采用固定模板与固定触发条件，支持失败重放；审批催办 / 超时提醒优先由钉钉承接，系统是否补充待实施前确认
- 知识库问答具备可追溯引用，支持基础文档运营

## 1.3 成功指标

| 指标 | 当前基线 | 一期目标 |
| :---- | :---- | :---- |
| 请假审批平均处理时长 | 约 2 天 | < 4 小时 |
| 报销流程处理时长 | 约 3 天 | < 1 天（不含线下打款时差） |
| 员工查文档耗时 | 5–30 分钟 | < 30 秒返回首答 |
| 通知遗漏率 | 人工不可控 | 0（系统触发链路） |
| 工作时段系统可用性 | — | ≥ 99% |

*统计口径：审批处理时长按工作时长计算（中国大陆法定工作日，工作时段 09:00–18:00，午休 12:00–13:30 不计时，时区 Asia/Shanghai）；工作时段内收到审批请求起计，节点状态变更时止。可用性仅统计工作时段。上述成功指标按自然日统计窗口（00:00–23:59）汇总。*

---

# 2. 范围定义

## 2.1 一期范围（必须交付）

- 请假审批（完整流程 + 状态机 + 通知）
- 报销审批（固定多级审批制度 + 节点状态追踪 + OCR + 凭证规则校验）
- 知识库问答（文档上传、切片索引、RAG 检索、来源引用）

## 2.2 一期不做（明确排除）

- 企业微信对话接入（仅预留接口）
- 可配置审批流引擎
- 销售线索管理
- 公章申请流程（移至二期）
- 工龄自动计算与复杂假种策略
- 外出审批流程接入（沿用钉钉既有流程与抄送规则）
- ERP（用友 / 金蝶）对接
- 移动 App（仅 Web 响应式）

## 2.3 二期预留

- 企业微信 Webhook 与账号映射
- 公章流程电子台账化
- 组织路由可配置（总经理 / 分公司负责人等）
- 自定义审批流与策略引擎

---

# 3. 关键业务决策（已确认）

| 决策项 | 结论 |
| :---- | :---- |
| 登录方式 | 统一采用钉钉 SSO；后端以服务端会话 + HttpOnly Cookie 维持登录态，不另建账号体系 |
| 用户生命周期 | 由钉钉同步，系统内不维护主数据 |
| 日历能力 | 统一采用中国大陆法定工作日历（休息按法定节假日），时区 Asia/Shanghai；仅用于 SLA/可用性统计，不用于请假时长自动换算 |
| 知识库规模 | 一期按 < 500 文档设计，架构支持扩展至 2000+ |
| 报销审批层级 | 完全以钉钉报销模板/流程为准；当前制度口径为固定串行多级审批，缺一不可，不得越级 |
| 报销总经理审批节点 | 当前统一走总经理审批，不做分公司路由（二期处理） |
| 直属上级 / 组织关系异常 | 以钉钉组织架构为准；如存在缺失或异常，需先在钉钉修正，一期系统不保留兜底审批人 |
| 请假审批层级 | 由钉钉请假模板固定并执行；系统仅负责发起、同步、展示，不额外干预审批节点 |
| 外出审批层级 | 由钉钉外出模板固定并执行；一期系统不接入，仅沿用钉钉既有流程与抄送 |
| 请假假种与粒度 | 年假（天/半天）、病假（天/半天）、事假（小时）、调休（小时，按余额）、婚假（天）、产假（天）、陪产假（天）、丧假（天）、哺乳假（小时）、产检假（天） |
| 请假余额维护 | 仅 HR 专员可在后台维护，管理员不可修改余额 |
| 余额不足处理 | 系统提示余额不足，但允许员工继续提交；审批单中标注余额不足，由审批人决定是否通过 |
| 请假最小申请单位 | 小时粒度假种（事假、调休、哺乳假）最小单位 1 小时；天/半天粒度假种最小单位 0.5 天 |
| 跨工作日计算 | 一期不做自动工作日计算，以员工填写的起止日期和时长为准，HR 审批时人工复核 |
| 报销拒绝后处理 | 员工可在原单修改后重新提交，状态从 REJECTED 回到 DRAFT，并保留 revision 历史 |
| 报销表单类型 | 差旅报销与日常报销使用两张独立表单，字段不同 |
| 报销分类口径 | 固定两类：差旅报销、日常报销 |
| 付款截图要求 | 不强制上传；如未上传，由财务在审批环节人工核查其他付款凭证（如转账记录） |
| 丢失原票处理 | 支持，Agent 引导员工填写书面说明，报销金额上限为原票金额 50%，需提供替票，走正常审批流 |
| 报销链路执行位置 | 全链路（含出纳挂款、会计复核）均在钉钉审批流内完成，系统不自行定义节点 |
| 特殊情况处理 | 仅允许邮件说明及批复留痕，不改变审批顺序，不允许越级或绕过节点；一期系统仅记录“已邮件说明 / 已邮件批复”标记或备注 |
| 审批超时提醒 | 优先由钉钉承接；系统是否补充提醒待实施前确认 |
| 撤回规则 | 仅 APPROVING 状态允许撤回，撤回后置为 CANCELLED |
| 知识库版本识别键 | 文件名 + 业务分类；业务分类由管理员上传时手动选择 |
| 状态机定位 | 结果同步模型：系统仅维护粗粒度业务状态，不控制审批节点流转；系统角色权限不代表审批链定义 |

---

# 4. 角色与权限

| 功能模块 | 员工 | 部门经理 | 会计 | 财务总监 | 总经理 | HR专员 | 管理员 |
| :---- | :----: | :----: | :----: | :----: | :----: | :----: | :----: |
| **发起请假** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **发起报销** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **查看本人单据** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **查看全员报销视图** | ❌ | ❌ | ✅ | ✅ | ✅ | ❌ | ✅ |
| **请假余额维护** | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| **知识库文档管理** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| **手动补偿操作** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| **审计日志查看** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |

*注：本表仅表达系统后台权限与数据范围，不表达审批链定义。审批节点顺序、审批人来源、抄送规则均以钉钉模板 / 流程为准。出纳可与会计为同一账号角色（小团队兼岗），但系统中保留独立节点状态字段。*

---

# 5. 功能需求

## 5.1 请假 Agent

### 5.1.1 功能描述

员工通过自然语言发起请假，Agent 以多轮对话方式承接钉钉既有请假表单字段与选项，确认假种、日期/时间段和时长后调用钉钉 API 创建审批实例；审批结果通过钉钉回调同步系统状态，并自动触发邮件通知。

一期不开放“保存草稿箱”。员工未确认提交前仅处于会话收集阶段，不生成员工可见的 DRAFT 单据。

### 5.1.2 请假多轮对话交互流程

请假流程分为三步，Agent 主动引导，不要求员工一次说清楚；底层字段与选项严格映射到钉钉现有请假表单，不重新设计假种或审批口径。

**第一步：识别或询问假种**

- 若员工消息中已包含明确假种（如"请年假"），直接进入第二步
- 若未包含，Agent 展示可用假种列表供员工选择，格式示例：

```
您想使用哪种假期？

• 年假     剩余 5 天
• 病假     剩余 10 天
• 事假     剩余 —
• 调休     剩余 3 小时
• 婚假 / 产假 / 陪产假 / 丧假 / 哺乳假 / 产检假

（余额数据来源：HR 后台维护值）
```

**第二步：根据假种确认日期 / 时间字段**

| 假种 | 对应填写方式 | 最小单位 |
| :---- | :---- | :---- |
| 年假 | 选择日期；若为半天，必须选择上午 / 下午 | 0.5 天 |
| 病假 | 选择日期；若为半天，必须选择上午 / 下午 | 0.5 天 |
| 婚假 / 产假 / 陪产假 / 丧假 | 选择起止日期 | 1 天 |
| 事假 | 填写开始时间 + 结束时间 | 1 小时 |
| 调休 | 填写开始时间 + 结束时间 | 1 小时 |
| 哺乳假 | 填写开始时间 + 结束时间 | 1 小时 |
| 产检假 | 选择起止日期 | 1 天 |

Agent 根据假种自动限定可填字段，不展示不适用选项。

**第三步：汇总确认或冲突重问**

Agent 展示汇总信息供员工确认，格式示例：

```
请确认您的请假申请：

假种：年假
时间：2026-03-26（明天）上午（0.5 天）
当前余额：5 天 → 申请后剩余：4.5 天

确认提交？
```

员工确认后，Agent 调用钉钉创建审批实例并返回单号。

若员工输入的日期、时间段、时长彼此冲突（例如“明天下午请半天假 4 小时”），Agent 不得直接提交，必须明确指出冲突并重新询问，直至字段可映射到钉钉表单。

**余额不足时的处理：**

```
提示：您的调休余额为 2 小时，本次申请 4 小时，余额不足。
是否仍要提交？提交后审批单中将标注余额不足，由审批人决定是否通过。
```

### 5.1.3 用户故事与验收

| 编号 | 角色 | 用户故事 | 验收标准 |
| :---- | :---- | :---- | :---- |
| L-001 | 员工 | 我想请假 | Agent 展示假种列表及各假种余额，引导员工选择 |
| L-002 | 员工 | 我想请年假 | 直接进入时长确认步骤，展示年假余额，限定为日期 + 天/半天；半天必须选择上午/下午 |
| L-003 | 员工 | 我想请明天上午的年假 | 识别假种（年假）、日期（明天）、粒度（半天/上午），汇总后请员工确认，确认后创建审批单 |
| L-004 | 员工 | 查看我本月请假记录 | 返回本月所有已创建请假单及状态（APPROVING / APPROVED / REJECTED / CANCELLED） |
| L-005 | 员工 | 我还有多少年假 | 返回 HR 后台维护的余额值，注明数据来源 |
| L-006 | 员工 | 调休只剩 1 小时但想请 4 小时 | 提示余额不足，询问是否继续提交；确认后创建审批单并在单据中标注余额不足 |
| L-007 | 系统 | 审批结果自动通知 | 钉钉回调触发后，2 分钟内发送邮件通知申请人（通过 / 拒绝均通知） |
| L-008 | 员工 | 我说的日期和时长对不上 | Agent 指出冲突字段，不得直接提交，必须重新确认 |

### 5.1.4 业务规则

- 支持假种与填写方式见 5.1.2 第二步字段表
- 不做工龄自动计算，不做复杂假种规则引擎
- 余额仅做提示，不强制拦截，以 HR 专员后台手动维护值为准（管理员无修改权限）
- 余额不足时允许提交，审批单标注余额不足，由审批人决定
- 请假 Agent 为对话化承接钉钉表单，字段口径、审批层级、抄送规则均以钉钉模板控制，系统不额外定义审批节点
- 一期不开放请假草稿箱；未确认提交前仅为会话态，不生成员工可见草稿
- 节假日 / 非工作时段是否允许提交，完全沿用钉钉现有表单与制度配置，Agent 不额外限制
- 跨工作日计算一期不自动处理，以员工填写为准，HR 审批时人工复核

---

## 5.2 财务报销 Agent

### 5.2.1 审批渠道与制度

报销与付款审批统一通过钉钉财务模块执行，固定串行审批链路以钉钉既有报销模板/流程为准；出纳挂款与会计复核节点同样在钉钉审批流内执行。系统负责对话收集、审批实例创建、状态同步与展示，不自行定义审批节点。

**当前制度口径（一期适用，实际执行以钉钉模板为准）**

```
报销人提交 → 部门经理审批 → 会计审批 → 财务总监审批 → 总经理审批 → 出纳挂款 → 会计复核 → 结束
```

- 审批顺序、审批人来源、抄送规则以钉钉组织架构与模板配置为准
- 特殊情况仅可邮件说明及批复留痕，不改变审批顺序，不允许越级或绕过节点

### 5.2.2 报销表单定义

报销 Agent 的定位与请假 Agent 一致：以多轮对话方式承接钉钉既有报销表单，不重新设计报销分类、字段口径与审批链。

**差旅报销表单专属字段：**

| 字段 | 必填 | 说明 |
| :---- | :---- | :---- |
| 出差起始地点 | ✅ | 如：沈阳 → 北京 |
| 出差起止日期 | ✅ | — |
| 出差事由 | ✅ | — |
| 同行人员 | ❌ | 多人出差时填写；一期仅做记录，不参与住宿、餐补、油费自动计算 |
| 交通费明细 | ✅ | 类型（火车/飞机/打车等）、金额、起止地点 |
| 住宿费 | ❌ | 金额、天数、城市（校验标准：其他城市 ≤200元/天，北上广深 ≤300元/天） |
| 餐补申请 | ❌ | 天数、金额（按 70 元/天计算；单次差旅餐补累计 ≤160 元无需发票，>160 元需上传金额高于餐补实际报销金额的替票） |
| 其他费用 | ❌ | 类型 + 金额 + 说明 |
| 发票 / 凭证上传 | ✅ | 至少一张，支持图片 / PDF |
| 合计金额 | ✅ | 系统自动汇总，不得超出所附票据金额（差旅餐补累计 ≤160 元免票除外） |
| 备注 | ❌ | — |

**日常报销表单专属字段：**

| 字段 | 必填 | 说明 |
| :---- | :---- | :---- |
| 费用类型 | ✅ | 见下方费用类型字典 |
| 费用发生日期 | ✅ | — |
| 费用归属客户 / 项目 | ❌ | 有则填写 |
| 报销事由 | ✅ | 不得使用含糊词语（如"日杂用品"），必须写明具体内容 |
| 金额 | ✅ | — |
| 发票 / 凭证上传 | ✅ | 至少一张 |
| 费用说明（按类型动态显示）| 视类型 | 见下方各类型补充要求 |
| 合计金额 | ✅ | 系统自动汇总 |

**日常报销费用类型字典及补充要求：**

| 费用类型 | 必填补充说明 |
| :---- | :---- |
| 打车费 | 起始地点 + 打车原由 |
| 油费 | 起始地点 + 公里数 + 开车原由（按 0.8 元/公里计算） |
| 快递费 | 邮寄的具体物品 |
| 招待费 | 招待事件或物品、招待对象、人数 |
| 办公用品 | 具体物品名称及数量（不得填"办公用品"等含糊描述） |
| 住宿费 | 金额、天数（非差旅场景） |
| 其他 | 具体说明，不得留空 |

### 5.2.3 报销凭证规则（来源：财务制度 YX-CW-ZD-002A）

以下规则在提交时由 Agent 引导员工完成，无法自动校验的项由财务在审批环节人工核查：

**Agent 提交时校验（系统可判断）：**

- 必须上传发票图片，不得跳过
- 报销合计金额不得超出所附全部票据金额（差旅餐补累计 ≤160 元免票除外）
- 差旅餐补累计金额 >160 元时，须上传金额高于餐补实际报销金额的替票后方可提交（例如餐补报销 170 元，替票可为 175 元）
- OCR 识别金额与申报金额不一致时，系统提示并要求填写差异原因后方可提交
- 发票去重按严格唯一口径处理：识别为同一张发票时不允许再次报销；电子票按“发票代码 + 发票号码 + 金额 + 日期”去重，OCR 失败时转人工复核
- 禁止使用预付卡票据报销（福利、招待除外），Agent 提示此规则

**财务审批时人工核查（系统不做自动校验）：**

- 发票抬头（公司全称）是否与付款单位一致
- 票据是否存在涂改或毁损
- 付款方式是否为转账（不可现金）；付款截图不强制，未上传时由财务核查其他付款凭证（如转账记录）
- 增值税专用发票是否同时提交发票联和抵扣联

**丢失原票特殊场景：**

- 员工告知 Agent 发票丢失时，Agent 引导填写书面说明（丢失原因、金额、业务背景）
- 报销金额上限自动设为原票金额的 50%
- 需提供替票并上传
- 经部门领导审批同意后方可报销，审批流程不变

### 5.2.4 差旅费标准（系统校验提示，不强制拦截）

| 项目 | 标准 |
| :---- | :---- |
| 火车 | 硬席 / 二等座 |
| 飞机 | 经济舱 |
| 住宿（其他城市） | ≤ 200 元/人/天 |
| 住宿（北上广深） | ≤ 300 元/人/天 |
| 餐补 | 早餐 10 元、午餐 30 元、晚餐 30 元，共 70 元/天 |
| 餐补发票要求 | 单次差旅餐补累计 ≤160 元无需发票；>160 元需补充金额高于餐补实际报销金额的替票 |
| 油费 | 0.8 元/公里 |

*超标时 Agent 给出提示，但允许员工填写超标原因后继续提交，由审批人决定。多人差旅场景下一期仅按申请人本人做自动提示与计算，同行人员不参与自动校验。*

### 5.2.5 用户故事与验收

| 编号 | 角色 | 用户故事 | 验收标准 |
| :---- | :---- | :---- | :---- |
| E-001 | 员工 | 我要报销打车费 58 元 | Agent 引导选择日常报销 → 打车费类型，提示填写起始地点和原由，上传发票后创建钉钉审批单 |
| E-002 | 员工 | 我要报销出差费用 | Agent 引导进入差旅报销表单，按字段逐步收集信息 |
| E-003 | 员工 | 查看我本月报销状态 | 返回所有单据与当前节点名称（current_node_name） |
| E-004 | 员工 | 上传发票自动填单 | OCR 自动提取字段并预填，允许人工修正后提交 |
| E-005 | 财务 | 查看全员报销汇总 | 可查总额、按差旅/日常分类统计、各节点待处理数量 |
| E-006 | 员工 | 报销被拒后我想修改重提 | 在原业务单据上修改后重新提交，保留 revision 历史，默认展示最新版本 |
| E-007 | 员工 | 发票丢了但需要报销 | Agent 引导填写书面说明，金额上限设为原票 50%，提示需上传替票，走正常审批流 |
| E-008 | 员工 | 住宿费超标了 | Agent 提示超出标准（如"北上广深住宿标准 ≤300元/天"），引导员工填写超标原因后允许提交 |
| E-009 | 员工 | 我上传了一张已经报销过的电子发票 | 系统按发票代码 + 号码 + 金额 + 日期识别重复并拒绝提交 |

### 5.2.6 报销规则补充

- 报销事由不得使用含糊或概括性词语（如"办公用品""日杂用品"等），Agent 识别后提示重新填写
- 招待费须写明招待事件或物品、对象及人数
- 打车费须注明起始地点及打车原由
- 汽油费须注明起始地点、公里数、开车原由
- 快递费须注明邮寄的具体物品
- 对借款进行报销冲抵的，需在备注中写明借款金额、冲抵金额、需还款金额
- OCR 失败时标记 `ocr_failed`，员工可手动录入全部字段后继续提交；系统不据此放弃人工复核
- 单据被拒绝后，员工可修改原业务单据重提；修改后原 `approval_instance_id` 作废，钉钉重新创建新实例，并保留 revision 历史
- 特殊情况邮件留痕，一期仅记录“已邮件说明 / 已邮件批复”的标记或备注，不采集邮件正文和附件

---

## 5.3 知识库 Agent

### 5.3.1 功能描述

支持 Word / PDF 上传、自动切片与向量索引，员工以自然语言提问，Agent 检索相关内容后返回答案，并统一附来源文档名、业务分类、上传时间及引用定位；其中 PDF 使用页码定位，Word 使用段落 / 标题定位。

### 5.3.2 用户故事与验收

| 编号 | 角色 | 用户故事 | 验收标准 |
| :---- | :---- | :---- | :---- |
| K-001 | 员工 | 公司年假政策怎么规定 | 返回相关条款，附文档名、业务分类、上传时间及引用定位 |
| K-002 | 员工 | XX 产品最新报价是多少 | 返回结果并标注来源文档名称、业务分类、上传时间及引用定位 |
| K-003 | 员工 | 我想看原文 | 支持查看来源段落 / 页码，可点击展开 |
| K-004 | 管理员 | 上传新版产品手册 | 旧版本标记为历史版本不删除，新版本立即生效并重建索引 |

### 5.3.3 知识库规则

- 上传权限仅管理员，员工只有查询权限
- 支持格式：PDF、Word（.docx / .doc）
- 文档版本识别键为「文件名 + 业务分类」；业务分类由管理员上传时手动选择。同键文档上传新版本后，旧版本标记历史版本，不物理删除
- “最新”判定规则：同一业务分类下，按上传更新时间最新版本
- 统一引用格式为「文档名 + 业务分类 + 上传时间 + 引用定位」；PDF 用页码，Word 用段落 / 标题定位
- 低置信度答案必须提示用户查看原文，不直接给出确定性结论
- 若知识库中完全无相关文档，明确返回【未找到相关文档】，不以 LLM 通用知识兜底（防止员工误以为是公司政策）
- 知识库为公共资源，不视为个人数据；查询权限一期不做文档级细分，全员均可查所有已上传文档
- 一期仍全员可查，但管理员只能上传公共内部资料，不允许上传涉密 / 敏感文档

---

# 6. 状态机定义

**设计原则：结果同步模型**
- 系统状态机为唯一权威业务状态，仅维护粗粒度结果状态
- 钉钉仅作为审批执行渠道，节点信息作为扩展展示字段，不参与状态流转
- 判断标准：系统可独立记录业务单据与失败结果；审批推进依赖钉钉审批实例创建成功（失败按重试策略处理）

## 6.1 请假单 & 报销单统一状态枚举

| 状态值 | 中文含义 | 说明 |
| :---- | :---- | :---- |
| DRAFT | 草稿 | 报销单可在拒绝后回到 DRAFT 进行修改重提；请假一期不开放员工可见草稿箱 |
| APPROVING | 审批中 | 已提交至钉钉，流程进行中 |
| APPROVED | 已通过 | 钉钉全流程审批通过 |
| REJECTED | 已拒绝 | 任意审批节点拒绝；员工可修改原单后重提（状态回到 DRAFT） |
| CANCELLED | 已撤销 | 申请人在 APPROVING 状态下主动撤回 |

## 6.2 数据库扩展字段

| 字段名 | 类型 | 说明 |
| :---- | :---- | :---- |
| status | VARCHAR | 主状态，唯一业务判断依据，取值见 6.1 |
| approval_instance_id | VARCHAR | 钉钉审批实例 ID，用于回调关联与问题排查 |
| current_node_name | VARCHAR | 当前审批节点名称（展示用），不参与状态流转 |
| current_approver | VARCHAR | 当前审批人姓名（展示用），不参与状态流转 |
| last_synced_at | TIMESTAMP | 最后同步时间，轮询兜底依据（超过 10 分钟触发主动拉取） |
| rejection_reason | TEXT | 拒绝原因（来自钉钉审批备注），REJECTED 状态时必填展示 |

## 6.3 状态更新机制

| 触发来源 | 操作 | 优先级 / 时机 |
| :---- | :---- | :---- |
| 钉钉回调 | 更新 status + current_node_name + current_approver + last_synced_at | 主路径，实时触发 |
| 定时轮询 | 查询 last_synced_at 超过 10 分钟未更新的 APPROVING 单据，主动拉取钉钉最新状态 | 兜底，每 10 分钟执行一次 |
| 管理员手动补偿 | 后台问答式流程确认影响范围与原因后，仅允许将 APPROVING 修正到 APPROVED / REJECTED / CANCELLED；操作必须记录 audit_log（含操作人、时间、原因） | 最后兜底，留审计轨迹 |

## 6.4 前端展示策略

| 页面 | 展示内容 | 数据来源 |
| :---- | :---- | :---- |
| 列表页 | 审批中 / 已通过 / 已拒绝 | status 字段 |
| 详情页 | 审批中（当前：会萍） | status + current_node_name |
| 拒绝详情 | 已拒绝（原因：金额超限） | status + rejection_reason |

---

# 7. 异常处理与幂等

## 7.1 钉钉回调幂等与安全

- 幂等键：`approval_instance_id + event_id` 组合唯一键
- 收到回调后先查幂等表，已处理则直接返回 200，不重复更新状态
- 回调乱序处理：以 `event_time` 最新为准，忽略旧事件
- 回调请求必须通过钉钉签名验签；验签失败直接拒绝并记录安全日志
- 回调时间戳校验：允许误差 ±5 分钟，超窗请求拒绝
- 回调 `nonce` 防重放：同一 `nonce` 在有效窗口内仅接受一次

## 7.2 审批创建幂等

- 业务单号作为幂等键，防止同一草稿重复创建钉钉审批实例
- 创建前检查该单号是否已有 `approval_instance_id`，有则直接返回已有实例
- 创建钉钉审批实例失败时自动重试最多 3 次（指数退避：1 分钟 / 3 分钟 / 10 分钟）；3 次仍失败后提示用户稍后重试

## 7.3 OCR 失败处理

- OCR 失败时标记字段 `ocr_failed = true`，前端提示用户手动录入
- 所有字段均已人工填写后，允许正常提交，`ocr_failed` 标记保留用于统计
- 财务查看时可筛选 `ocr_failed` 单据，酌情人工复核发票真实性

## 7.4 邮件失败处理

- 发送失败自动重试 3 次，指数退避（1 分钟 / 3 分钟 / 10 分钟）
- 3 次均失败后记录 `email_logs` 中 `status = failed`，管理员可在后台手动重放
- 邮件失败仅在后台日志与运维排查视图可见，不在业务单据详情中展示

## 7.5 知识库低置信度

- 置信度阈值由开发阶段调试确定（建议初始值 0.75），产品上线前复核
- 低于阈值时强制返回来源入口，不给出确定性结论
- 知识库无相关文档时明确返回【未找到相关文档】，禁止以 LLM 通用知识兜底

---

# 8. 通知规则

| 触发场景 | 通知对象 | 通知渠道 | 触发时机 |
| :---- | :---- | :---- | :---- |
| 审批通过 | 申请人 | 网易企业邮箱 | 钉钉回调后 < 2 分钟 |
| 审批拒绝 | 申请人 | 网易企业邮箱 | 钉钉回调后 < 2 分钟（含拒绝原因） |
| 新单待审批 | 当前审批节点的审批人 | 钉钉工作通知 | 创建审批实例后立即推送 |
| 审批超时未处理 | 当前审批人 | 优先由钉钉承接 | 系统是否补充提醒待实施前确认 |
| 请假/外出审批抄送 | 副总经理 / 总经理 / 人事 / 部门负责人（外出含财务） | 钉钉工作通知 | 由钉钉审批模板自动触发（系统不额外发送） |
| 申请人撤回 | 当前节点审批人（仅通知当前节点，不追溯已通过节点） | 钉钉工作通知 | APPROVING 状态撤回后立即推送 |

*注：所有邮件发送结果记录至 email_logs，失败自动重试 3 次，支持后台重放。审批超时提醒 / 催办优先由钉钉承接；系统是否补充提醒待实施前确认。一期不提供通知模板与触发条件后台配置。邮件失败仅在后台可见，不在业务单据详情中展示。*

---

# 9. 非功能需求

## 9.1 性能

- 普通对话响应：p95 < 5s
- 知识库问答：p95 < 8s
- 钉钉审批创建：p95 < 10s
- 邮件发送延迟：审批回调后 < 2 分钟
- 并发能力：支持 20 人同时在线
- 性能指标按自然日统计窗口（00:00–23:59）汇总

## 9.2 可用性

- 工作时段（09:00–18:00，午休 12:00–13:30 不计时）可用性 ≥ 99%
- 审批处理时长统计口径：仅统计工作时长（中国大陆法定工作日，时区 Asia/Shanghai）；工作时段内收到审批请求起计，节点状态变更时止
- 工作时段外、午休时段或法定休息日收到审批请求时，于下一计时窗口起计
- 外部依赖（钉钉 / 邮件）故障时系统给出可重试提示并保留失败记录
- 可用性指标按自然日统计窗口（00:00–23:59）汇总

## 9.3 安全与合规

- 最小权限原则：员工仅可见本人业务数据；知识库作为公共资源全员可查（上传仅管理员，且仅允许上传公共内部资料）
- 管理员手动补偿操作采用问答式确认并保留完整审计问答记录（最小必填：单据ID、变更前状态、变更后状态、变更原因；二次复核人一期非必填）
- 管理员手动补偿仅允许将 APPROVING 修正到 APPROVED / REJECTED / CANCELLED，已终态不得随意回退
- 新员工首次钉钉 SSO 登录后默认角色为【员工】，管理员可在后台调整角色
- 审计日志保留 180 天
- 对话日志保留 90 天
- 请假 / 报销记录保留 3 年；发票图片保留 5 年
- 发票图片存储：阿里云 OSS，启用加密存储，满足 5 年保留要求
- 敏感凭证（AppSecret / SMTP 密码 / API Key）仅存服务端环境变量，不写入代码或日志
- LLM API 调用不传身份证号、工资等敏感字段

---

# 10. 系统集成与技术架构

## 10.1 技术栈

| 层级 | 技术选型 | 说明 |
| :---- | :---- | :---- |
| 前端 | React + Vite + Tailwind CSS | 对话界面 + 管理后台 |
| 后端 | Node.js + Express | API 服务 + Agent 编排 + 回调处理 |
| 任务队列 | BullMQ + Redis（自建） | 邮件重试、轮询兜底、OCR 异步；审批催办是否补充由实施前确认 |
| LLM | 通义千问 qwen-plus（阿里云百炼） | 意图识别、对话管理、内容生成、Tool Use |
| Embedding | text-embedding-v3（阿里云百炼） | 知识库文档向量化，1536 维 |
| OCR | 阿里云发票识别 API（增值税发票专项） | 发票结构化字段提取 |
| 数据库 | 阿里云 RDS PostgreSQL + pgvector | 业务数据 + 向量索引 |
| 文件存储 | 阿里云 OSS | 发票图片（加密，5 年保留） |
| 邮件 | nodemailer + 网易企业邮箱 SMTP | 审批结果通知；失败可后台重放 |
| 审批 | 钉钉开放平台 API | SSO · 组织 · 审批创建 · 回调 · 工作通知 |
| 部署 | 阿里云 ECS（2核4G）+ PM2 + Nginx | 单机满足一期规模 |

## 10.2 钉钉集成要点

- 鉴权：企业内部应用，AppKey / AppSecret，存服务端环境变量；钉钉 SSO 成功后由后端创建服务端会话，使用 HttpOnly Cookie 维持登录态
- 读取：员工信息 / 组织架构 / 钉钉工作通知推送
- 写入：创建审批流实例（请假 / 报销）
- 审批责任边界：审批顺序、审批人来源、抄送规则均以钉钉模板 / 流程为准，系统不定义审批链
- 回调：接收审批结果事件，幂等键为 `approval_instance_id + event_id`，并执行签名验签 + 时间戳校验 + `nonce` 防重放
- 企业微信：一期不接入，仅预留扩展接口

## 10.3 核心数据表

- `users`：员工信息与角色（来源钉钉同步）
- `leave_requests`：请假单（含 leave_type / duration_unit / balance_warning / status / approval_instance_id / current_node_name / current_approver / last_synced_at）
- `expense_reports`：报销单（含 expense_category〔差旅/日常〕/ form_type / status 扩展字段 / rejection_reason / ocr_failed / lost_invoice_flag）
- `invoices`：发票元信息 + OCR 结构化结果 + OSS 图片路径 + invoice_number（去重校验）
- `documents`：知识库文档元信息（文件名 / 业务分类 / 版本 / 状态 / 上传时间）
- `document_chunks`：文档切片 + pgvector 向量索引
- `approval_logs`：钉钉回调原始事件记录（幂等表）
- `email_logs`：邮件发送日志（含重试次数 / 失败原因）
- `audit_logs`：系统操作审计（含管理员手动补偿操作）

---

# 11. 一期开发里程碑（25 天）

| 阶段 | 周期 | 交付内容 | 里程碑 |
| :---- | :---- | :---- | :---- |
| D1–D3 | 第 1–3 天 | 项目初始化、钉钉 SSO 接入、权限骨架、Orchestrator 路由基础 | 可登录，角色权限生效 |
| D4–D8 | 第 4–8 天 | 请假流程：多轮对话、假种余额展示、钉钉审批创建、回调、状态机、邮件通知 | 请假全链路跑通 |
| D9–D13 | 第 9–13 天 | 报销流程：差旅/日常双表单、OCR 上传、凭证规则校验、丢失原票场景、审批创建、状态同步、拒绝重提 | 报销全链路跑通 |
| D14–D18 | 第 14–18 天 | 知识库：文档上传解析、切片索引、RAG 问答、来源引用、低置信度提示 | 知识库可查询 |
| D19–D21 | 第 19–21 天 | 幂等保障、轮询兜底、手动补偿后台、审计日志、邮件重放 | 异常处理完备 |
| D22–D24 | 第 22–24 天 | 全链路联调、性能压测（p95 达标）、UAT 验收 | UAT 通过 |
| D25 | 第 25 天 | 上线部署与回滚预案演练 | 正式上线 |

---

# 12. UAT 验收门槛

- 请假多轮对话流程：假种列表正确展示余额 → 按假种限定字段 → 汇总确认 → 创建审批单
- 请假字段完全映射钉钉现有表单口径；半天必须选上午/下午，小时假必须填写开始时间与结束时间
- 请假冲突场景：日期 / 时间段 / 时长不一致时，Agent 必须指出冲突并重新确认，不得直接提交
- 余额不足场景：提示后允许提交，审批单标注余额不足
- 请假、报销均可创建钉钉审批并追踪全链路状态
- 报销节点顺序严格符合制度，实际执行以钉钉模板为准；拒绝后可在原业务单据上修改重提并保留 revision 历史
- 差旅报销与日常报销使用独立表单，字段分别正确展示
- 差旅费超标场景：系统提示超标标准，允许填写原因后提交
- 丢失原票场景：金额上限自动设为 50%，提示上传替票，走正常审批流
- 报销事由含糊词（"办公用品"等）：Agent 识别后提示重新填写
- 电子发票重复使用：系统按发票代码 + 号码 + 金额 + 日期识别重复并拒绝提交；OCR 失败时转人工复核
- 多人差旅场景：同行人员仅做记录，不参与住宿 / 餐补 / 油费自动计算
- 特殊情况仅允许邮件留痕，不得绕过既定审批节点；一期系统仅记录“已邮件说明 / 已邮件批复”标记或备注
- 请假假种与粒度符合一期口径（小时 / 半天 / 天；其中产检假按“天”）
- 状态展示：列表页显示粗粒度状态，详情页显示当前节点名称与审批人
- 撤回规则：仅 APPROVING 状态允许撤回并置为 CANCELLED；撤回通知仅推送当前节点审批人
- 审批催办 / 超时提醒优先由钉钉承接；系统是否补充提醒作为实施前确认项
- 管理员手动补偿：仅允许在明确异常时，将 APPROVING 修正到 APPROVED / REJECTED / CANCELLED，并留完整审计轨迹
- 报销分类统计仅包含【差旅报销】【日常报销】
- 回调幂等：重复推送同一事件不产生重复状态变更
- 回调安全：验签失败/时间戳超窗/`nonce` 重放请求均被拒绝并留痕
- 轮询兜底：模拟回调丢失，10 分钟内系统自动同步正确状态
- 知识库回答必须附“文档名 + 业务分类 + 上传时间 + 引用定位”；PDF 用页码，Word 用段落 / 标题定位
- 知识库无相关文档时返回【未找到】，不使用 LLM 通用知识兜底
- 知识库仅允许管理员上传公共内部资料，不允许上传涉密 / 敏感文档；一期仍全员可查
- 核心链路测试集（请假/报销/知识库主链路 + 回调幂等/轮询兜底/OCR失败/邮件重试/审批创建重试异常场景）覆盖不少于 120 条用例；P0 用例通过率 100%，整体通过率 ≥ 99%
- 并发压测按 20 人同时在线验收，关键链路性能指标达标
- 关键日志（审批回调 / 邮件发送 / 审计操作）可检索可追溯；邮件失败仅在后台可见
- 发票图片上传至阿里云 OSS，可正常读取

---

# 13. 版本结论

本版（v1.5）为当前唯一执行口径，替代所有历史版本。后续变更以版本号递增发布。

若制度调整涉及审批角色或顺序，统一在二期引入【组织路由 + 可配置审批流】后处理，一期不做临时补丁。

**二期待规划事项：**
- 企业微信 Webhook 接入（外部客服渠道）
- 公章申请流程电子台账化
- 销售支持 Agent（产品查询 / 报价 / 线索管理）
- 任务自动化（邮件起草 / 日历 / 周报纪要）
- 财务报表生成与可配置审批流引擎

---

*文档结束 · v1.5 · 后续变更请递增版本号*
